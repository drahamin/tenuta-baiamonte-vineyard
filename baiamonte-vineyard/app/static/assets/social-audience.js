function socialPeople(rows,emptyText){return rows.length?rows.map(row=>`<a href="${esc(row.profile_url||`https://www.instagram.com/${encodeURIComponent(row.username)}/`)}" target="_blank" rel="noopener"><b>@${esc(row.username)}</b><span>Open Instagram ↗</span></a>`).join(''):`<p>${esc(emptyText)}</p>`}
function renderSocialAudience(data){const audience=data.audience||{},summary=audience.summary||{},relationships=data.relationships||{},accounts=audience.accounts||[],events=audience.events||[],imports=relationships.imports||[],accountNode=$('socialAudienceAccounts'),historyNode=$('socialAudienceHistory');$('socialAudienceStatus').textContent=accounts.length?`${accounts.length} account${accounts.length===1?'':'s'} stored · 90-day history`:'Waiting for first live snapshot';accountNode.classList.toggle('empty',!accounts.length);accountNode.innerHTML=accounts.length?accounts.map(row=>`<article><span>${esc(row.platform)}</span><strong>${fmt(row.followers_count||0)}</strong><small>followers · ${row.following_count==null?'following not supplied':fmt(row.following_count)+' following'}<br>Captured ${timeLabel(row.captured_at)}</small></article>`).join(''):'No audience snapshots yet.';historyNode.classList.toggle('empty',!events.length);historyNode.innerHTML=`<div class="social-change-totals"><span><b>+${fmt(summary.net_follows_30d||0)}</b><small>net gains · 30d</small></span><span><b>−${fmt(summary.net_unfollows_30d||0)}</b><small>net losses · 30d</small></span><span><b>${Number(summary.net_change_30d||0)>0?'+':''}${fmt(summary.net_change_30d||0)}</b><small>net change · 30d</small></span></div><div class="social-change-list">${events.length?events.slice(0,12).map(row=>`<div class="${row.event_type==='net_follow'?'gain':'loss'}"><b>${row.event_type==='net_follow'?'+':'−'}${fmt(row.audience_change)}</b><span>${esc(row.platform)} · ${fmt(row.previous_count)} → ${fmt(row.current_count)}</span><time>${timeLabel(row.detected_at)}</time></div>`).join(''):'<p>No count change has been detected yet.</p>'}</div>`;$('socialAudienceNote').textContent=audience.identity_note||'Meta provides aggregate audience totals; individual identities require an official account export.';const cadence=$('socialExportCadence'),due=Boolean(relationships.export_due),next=relationships.next_export_due_at?new Date(relationships.next_export_due_at).toLocaleDateString():null;cadence.className=`social-export-cadence ${due?'due':'current'}`;cadence.innerHTML=`<div><b>${due?'Official export due':'Automatic 10-day check is current'}</b><span>${due?'Request the Followers and following JSON export in Meta Accounts Center. Once selected below, import and comparison are automatic.':`Next official export due ${esc(next||'in 10 days')}. Aggregate follower totals continue updating automatically.`}</span></div><a href="https://accountscenter.instagram.com/info_and_permissions/dyi/" target="_blank" rel="noopener">Open Accounts Center ↗</a>`;$('socialRelationshipSummary').classList.toggle('empty',!imports.length);$('socialRelationshipSummary').innerHTML=imports.length?`<b>Latest import: ${fmt(imports[0].followers_count)} followers · ${fmt(imports[0].following_count)} following</b><span>${new Date(imports[0].imported_at).toLocaleString()} · ${esc(imports[0].source_filename||'Meta export')}${imports.length>1?` · compared with ${new Date(imports[1].imported_at).toLocaleDateString()}`:''}</span>`:'No relationship export has been imported.';$('socialRecentUnfollowers').innerHTML=socialPeople(relationships.recent_unfollowers||[],'Import two exports to identify changes.');$('socialNotFollowingBack').innerHTML=socialPeople(relationships.not_following_back||[],'Everyone in the import follows you back.');$('socialNotFollowedBack').innerHTML=socialPeople(relationships.not_followed_back||[],'You follow everyone in the import.')}
function bindSocialAudience(){const importer=$('socialAudienceImport');if(!importer)return;importer.onsubmit=async event=>{event.preventDefault();const button=event.submitter,data=new FormData(importer),file=data.get('file');if(!file?.size)return toast('Choose a Meta Instagram export');button.disabled=true;button.textContent='Importing…';try{const result=await formApi('api/v1/social/audience-import',data);state.social.relationships=result.relationships;renderSocialAudience(state.social);importer.reset();toast(`Imported ${result.followers} followers and ${result.following} following`)}catch(error){toast(error.message)}finally{button.disabled=false;button.textContent='Import followers & following'}}}
